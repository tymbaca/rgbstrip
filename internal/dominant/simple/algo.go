package simple

// func Find(img image.Image) (color.RGBA, error) {
// 	width := img.Bounds().Dx()
// 	height := img.Bounds().Dy()

// 	colors := make([]color.RGBA, width*height)
// 	i := 0
// 	for y := range height {
// 		for x := range width {
// 			c := img.At(x, y)
// 			r, g, b, a := c.RGBA()
// 			colors[i] = color.RGBA{R: byte(r), G: byte(g), B: byte(b), A: byte(a)}

// 			i++
// 		}
// 	}

// }
